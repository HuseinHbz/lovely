/**
 * تنها جایی که یک رنگ خارج از `app/globals.css` به‌صورت لفظی نوشته می‌شود.
 *
 * `<meta name="theme-color">` متغیر CSS قبول نمی‌کند — مرورگر قبل از پارس شدن
 * استایل‌شیت رنگ نوار آدرس را می‌خواهد. پس مقدار `--color-shab` اینجا آینه شده.
 *
 * `node scripts/contrast.mjs` چک می‌کند این دو از هم جدا نیفتند و اگر افتادند
 * با کد خطا تمام می‌شود؛ یعنی این استثنا قابل فراموش شدن نیست.
 */
export const THEME_COLOR = '#101A2E';
