import { Museum } from '@/components/Museum';

/**
 * موزه‌ی خاطرات — فاز ۹.
 *
 * پشت دروازه است بدون اینکه کاری کند: `middleware.ts` هر مسیری جز فهرست
 * عمومی را می‌گیرد، و این مسیر در آن فهرست نیست.
 */
export default function MuseumPage() {
  return <Museum />;
}
